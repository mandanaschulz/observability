networks:
  observability-net:
    driver: bridge
services:

  fluent-bit:
    image: fluent/fluent-bit:3.2
    container_name: fluentbit
    depends_on:
      # postgres:
      #   condition: service_healthy
      loki:
        condition: service_started
    volumes:
      - ./fluent-bit/fluent-bit.conf:/fluent-bit/etc/fluent-bit.conf:ro
      - ./fluent-bit/parsers.conf:/fluent-bit/etc/parsers.conf:ro
      - ./fluent-bit/scripts:/fluent-bit/scripts:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /home/tethys/.ros/log:/ros-logs:ro  #.env ${ROS_LOG_PATH}
      - fluent-bit-storage:/var/log/fluent-bit/storage
    ports:
      - "8888:8888"
      - "2020:2020"
    networks:
      - observability-net
    restart: unless-stopped

  # # ─── ROS TOPICS  ───────────────────────────────────────────────

  # postgres:
  #   build: ./postgres
  #   container_name: postgres
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: changeme
  #     POSTGRES_DB: robot_analytics
  #   volumes:
  #     - /home/tethys/observability/data/postgres:/var/lib/postgresql/data #!!
  #     - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
  #   command: [
  #     "postgres", 
  #     "-c", "shared_preload_libraries=pg_cron", 
  #     "-c", "cron.database_name=robot_analytics"
  #   ]
  #   ports:
  #     - "5432:5432"
  #   networks:
  #     - observability-net
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres -d robot_analytics"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10
  #   restart: unless-stopped

  # ros2-bridge:
  #   build: ./ros2_bridge
  #   container_name: ros2_bridge
  #   network_mode: host 
  #   environment:
  #     ROS_DOMAIN_ID: "0" 
  #   depends_on:
  #     fluent-bit:
  #       condition: service_started
  #   volumes:
  #     - ./ros2_bridge/manifest.yaml:/app/manifest.yaml:ro
  #   command:
  #     - "--manifest"
  #     - "/app/manifest.yaml"
  #     - "--fluentbit-url"
  #     - "http://127.0.0.1:8888/" 
  #   restart: unless-stopped

  # ─── LOGS  ───────────────────────────────────────────────

  loki:
    image: grafana/loki:3.7.2
    container_name: loki
    ports:
      - "3100:3100"          
    volumes:
      - ./loki/loki.yaml:/etc/loki/loki-config.yaml:ro
      - /home/tethys/dev/observability/data/loki:/loki      
    command: ["-config.file=/etc/loki/loki-config.yaml"]
    networks:
      - observability-net
    restart: unless-stopped

# # ─── SYSTEM HEALTH  ───────────────────────────────────────────────

#   prometheus:
#     image: prom/prometheus:v3.4.0
#     container_name: prometheus
#     volumes:
#       - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
#       - ./prometheus/alert.rules:/etc/prometheus/alert.rules:ro
#       - /home/tethys/observability/data/prometheus:/prometheus
#     command:
#       - '--config.file=/etc/prometheus/prometheus.yml'
#       - '--storage.tsdb.path=/prometheus'
#       - '--storage.tsdb.retention.time=90d'
#       - '--storage.tsdb.retention.size=30GB'
#     ports:
#       - "9090:9090"
#     depends_on:
#       - cadvisor
#       - node-exporter
#     networks:
#       - observability-net
#     restart: unless-stopped

#   node-exporter:
#     image: quay.io/prometheus/node-exporter:v1.9.1
#     container_name: node-exporter
#     volumes:
#       - /proc:/host/proc:ro
#       - /sys:/host/sys:ro
#       - /:/rootfs:ro
#     command:
#       - '--path.rootfs=/rootfs'
#       - '--path.procfs=/host/proc'
#       - '--path.sysfs=/host/sys'
#       - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
#     ports:
#       - "9100:9100"
#     networks:
#       - observability-net
#     restart: unless-stopped

#   cadvisor:
#     image: gcr.io/cadvisor/cadvisor:v0.52.1
#     container_name: cadvisor
#     volumes:
#       - /:/rootfs:ro
#       - /var/run:/var/run:ro
#       - /sys:/sys:ro
#       - /var/lib/docker:/var/lib/docker:ro
#     ports:
#       - "8080:8080"
#     networks:
#       - observability-net
#     restart: unless-stopped

volumes:
  fluent-bit-storage:
  grafana-data:


# alertmanager:
#   image: prom/alertmanager:v0.28.1
#   container_name: alertmanager
#   volumes:
#     - ./alertmanager/config.yml:/etc/alertmanager/config.yml:ro
#   command:
#     - '--config.file=/etc/alertmanager/config.yml'
#     - '--storage.path=/alertmanager'
#   ports:
#     - "9093:9093"
#   networks:
#     - observability-net
#   restart: unless-stopped

grafana:
  image: grafana/grafana:12.0.1
  container_name: grafana
  depends_on:
    #- prometheus
    - loki          
  ports:
    - "3000:3000"
  volumes:
    - grafana-data:/var/lib/grafana
    - ./grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
    - ./grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
  environment:
    - GF_SECURITY_ADMIN_USER=admin
    - GF_SECURITY_ADMIN_PASSWORD=changeme
    - GF_USERS_ALLOW_SIGN_UP=false
  networks:
    - observability-net
  restart: unless-stopped